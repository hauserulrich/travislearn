language: node_js
node_js:
  - "8"
#setting up VM
before_install:
#activities to be performed during building
jobs:
  include:
    -stage: lint
    #lint all html files in tree
    script: node node_modules/htmllint-cli/bin/cli.js ./www/**/*index.html
